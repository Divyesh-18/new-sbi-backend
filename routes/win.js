// var express = require('express');
// var router = express.Router();
// const verifyToken = require('../middleware/auth');
// const winController = require("../controllers/win");
// const requestValidator = require('../middleware/requestValidator.middaleware');
// const validator = require('../validators/auth.validator');

// router.post('/batenow', requestValidator(validator.batenowSchema), winController.batenow);
// router.post('/demo', winController.demo);
 

// module.exports = router;